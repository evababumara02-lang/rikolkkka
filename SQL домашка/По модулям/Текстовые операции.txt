-- задача 1
select * 
from quests q 
where q.quest_name  ILIKE '%dragon%';

-- задача 2
select *
from quests q 
where q.quest_name like 'Rescue%';

-- задача 3
select *
from quests q 
where q.quest_name like '%Duloc';

-- задача 4
select q.quest_name, LENGTH (q.quest_name) len
from quests q 
order by len desc, q.quest_name;

-- задача 5
select quest_id, quest_name || ' | reward=' || reward_gold label
from quests q 
where q.reward_gold is not null;

-- задача 6
select * 
from quests q 
where q.quest_name ilike '%save%' or quest_name ilike '%rescue%';

-- задача 7
select quest_id, substring (quest_name,1,5) 
from quests q
limit 5

-- задача 8
select quest_id, replace (quest_name, '','-') as dash_title
from quests q 

-- задача 9
select *
from quests q 
where LENGTH (q.quest_name)> (select avg (LENGTH(quest_name )) from quests)

-- задача 10
SELECT quest_id ,POSITION('swamp' IN LOWER(quest_name)) pos
  FROM quests