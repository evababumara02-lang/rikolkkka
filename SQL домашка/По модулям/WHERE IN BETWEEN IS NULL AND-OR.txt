-- заданиие 1
SELECT * FROM characters
WHERE species IN ogre

-- задание 2
SELECT event_id, event_type, risk_level FROM events 
WHERE risk_level < 4

-- задание 3
SELECT * FROM locations
WHERE type IN swamp,forest 
ORDER BY type, name

-- задание 4
SELECT name, strenght_level  FROM characters
WHERE strength_level BETWEEN 4 and 6

-- задание 5
SELECT name, alignment FROM characters 
WHERE alignment <> good

-- задание 6
SELECT  quest_id, quest_name, reward_gold FROM quests 
WHERE reward_gold > 100

-- задание 7
SELECT quest_id, quest_name FROM quests 
WHERE reward_gold IS NULL

-- задание 8
SELECT event_id, character_id FROM event_participants
WHERE event_participants=victim

-- задание 9
SELECT * FROM events
WHERE start_day = 5

-- задание 10
SELECT name, intelligence_level, is_magical, alignment, strength_level FROM characters 
WHERE is_magical and intelligence_level ≥ 7 OR alignment = 'evil' и strength_level ≥ 6

